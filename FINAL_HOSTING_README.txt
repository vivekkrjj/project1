BHUMI NURSING COLLEGE - FINAL VERCEL HOSTING PACKAGE

IMPORTANT:
1. Upload the CONTENTS of this ZIP as the Vercel project root. Do not put this ZIP inside another folder.
2. The HTML pages now use root-level CSS/JS/logo fallbacks to avoid broken /css, /js or /assets paths on static hosting.
3. Root copies: style.css, portal.css, home-style-root.css, logo.png and JS files are intentional.
4. Existing Supabase SQL files and functionality are retained.
5. After deployment, hard-refresh the browser (Ctrl+F5) once.
6. Student ID Card remains download-only in the portal UI.
